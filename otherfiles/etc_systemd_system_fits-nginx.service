# This is a modified Centos7 httpd.service

[Unit]
Description=The NGINX FitsService Process
After=network.target remote-fs.target nss-lookup.target

[Service]
Type=forking
ExecStart=nginx -g daemon off;
PrivateTmp=true

[Install]
WantedBy=multi-user.target
